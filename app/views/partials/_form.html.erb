<%= form_for(:idea, url: api_v1_ideas_path) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Save", id: 'save-idea' %>
<% end %>
