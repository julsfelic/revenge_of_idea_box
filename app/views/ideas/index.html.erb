<ul class="ideas">
  <% if @ideas.empty? %>
    <li>
      <h1>Add an idea!</h1>
    </li>
  <% else %>
    <% @ideas.each do |idea| %>
      <li data-id=<%= idea.id %>>
        <h1><%= idea.title %></h1>
        <h2><%= idea.body %></h2>
        <h3><%= idea.quality %></h3>
        <%= link_to 'Delete', '#', class: 'delete-idea' %>
        <%= link_to image_tag('thumbs_up', alt: 'thumbs up'), '#', class: 'thumbs_up' %>
        <%= link_to image_tag('thumbs_down', alt: 'thumbs down'), '#', class: 'thumbs_down' %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= render partial: "partials/form" %>
